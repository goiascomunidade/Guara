# Registry

Community tool manifests should be proposed here and validated in CI.

Suggested model:
- `tool_id/manifest.json`
- semantic versioning with compatibility matrix
- security review before publication
